---
import { cn } from "@/lib/utils";
import { Icon } from "astro-icon/components";
import { links } from "@config";

interface Props {
    class?: string;
}

const { class: className } = Astro.props;
---

<header class="w-full max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
    <nav
        class={cn(
            "max-w-5xl w-full flex items-center justify-between px-4 md:px-6 mx-auto border-b py-6",
            className
        )}>
        <a
            href={links.home}
            class="flex-none rounded-xl text-xl inline-block font-bold focus:outline-none focus:opacity-80 hover:opacity-80 transition">
            DigiDisha
        </a>

        <a
            href={links.zenithxtech}
            target="_blank"
            rel="noopener noreferrer"
            class="flex items-center gap-1 py-2 px-4 text-sm font-medium rounded-xl border border-transparent bg-primary text-background hover:bg-primary/90 focus:outline-none focus:bg-primary/90 transition text-nowrap">
            zenithxtech
            <Icon name="tabler:trending-up" />
        </a>
    </nav>
</header>
